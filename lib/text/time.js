'use strict'

// dependencies
var TextElement = require('./element')

/**
 * Paragraphs
 * @memberOf UIKit
 */
module.exports = new TextElement({
  node: 'time',
  ui: {
    name: 'time',
    atomic: 'atom'
  }
}).Constructor
